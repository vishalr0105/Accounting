import { OnlydigitsDirective } from './onlydigits.directive';

describe('OnlydigitsDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlydigitsDirective();
    expect(directive).toBeTruthy();
  });
});
